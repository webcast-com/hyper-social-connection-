/**
 * Google Fonts — Inter (UI) + Poppins (headings / brand).
 *
 * Loaded from the Google Fonts CSS API at runtime (preconnect + stylesheet)
 * rather than `next/font/google`, which downloads faces at compile time and
 * fails in offline / restricted networks. The browser caches the files;
 * Tailwind maps the family names in `src/app/globals.css`.
 */
export const GOOGLE_FONTS_STYLESHEET =
  'https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Poppins:wght@500;600;700;800&display=swap';
